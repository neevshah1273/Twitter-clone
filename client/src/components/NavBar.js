import React from 'react';
const pages = ['Search', 'Notifications', 'Tweet'];
const settings = ['Profile', 'Logout'];

const NavBar = () => {
  


  return (
    <nav>

    </nav>
  );
};
export default NavBar;